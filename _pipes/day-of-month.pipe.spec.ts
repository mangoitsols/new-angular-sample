import { DayOfMonthPipe } from './day-of-month.pipe';

describe('DayOfMonthPipe', () => {
  it('create an instance', () => {
    const pipe = new DayOfMonthPipe();
    expect(pipe).toBeTruthy();
  });
});
