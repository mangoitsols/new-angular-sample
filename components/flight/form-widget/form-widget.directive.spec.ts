import { FormWidgetDirective } from './form-widget.directive';

describe('FormWidgetDirective', () => {
  it('should create an instance', () => {
    const directive = new FormWidgetDirective();
    expect(directive).toBeTruthy();
  });
});
